import React from 'react';
// eslint-disable-next-line
import PropTypes from 'prop-types';

export const Login = () => {
    return(
        <div>
            Login
        </div>
    )
}

Login.propTypes = {
    
};

export default Login
