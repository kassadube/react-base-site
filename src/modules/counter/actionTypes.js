import {NAME} from './constants';

export const INCREMENT_REQUESTED = `${NAME}/INCREMENT_REQUESTED`;
export const INCREMENT = `${NAME}/INCREMENT`;
export const DECREMENT_REQUESTED = `${NAME}/DECREMENT_REQUESTED`;
export const DECREMENT = `${NAME}/DECREMENT`;